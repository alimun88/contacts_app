<h1 class="center">Welcome to <%= @user.full_name %>'s page</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3 center">
    <%= gravatar_for(@user, size: 150) %>
  </div>
</div>
    <h4 class="center"> <%= @user.username %>'s contacts</h4>
<div class="center">
  <%= will_paginate @user_contacts %>
</div>
  <%= render 'contacts/contact', obj: @user_contacts %>
<div class="center">
  <%= will_paginate @user_contacts %>
</div>